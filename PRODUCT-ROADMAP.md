# Tripboard 功能大纲

这个文档用于防止后续迭代遗忘和跑偏。每完成一项，就在这里同步勾选并补充状态。

## 状态说明

- `[x]` 已完成并已推送公网
- `[ ]` 未完成
- `[~]` 部分完成，仍有后续依赖或升级项

## 功能清单

### 1. 交通票价

- [~] 官方查询入口
  - 已完成：携程查询、Trip.com 查询、12306 查询入口。
  - 已完成：根据当前出发地、目的地、日期生成查询关键词。
- [~] 手动保存报价
  - 已完成：可手动保存航班/动车班次、时间、价格到当天交通列表。
  - 已完成：手动报价会进入协作计划并同步。
- [~] Supabase 交通代理
  - 已完成：前端可调用 Supabase Edge Function。
  - 当前限制：无正式 Trip.com/Ctrip API 权限时只返回代理示例。
- [~] Amadeus 个人开发者航班 API
  - 已完成：新增 Supabase Edge Function `amadeus-flight-offers`，用于安全调用 Amadeus OAuth 和 Flight Offers Search。
  - 已完成：前端接入面板已改为 Amadeus 航班报价测试，支持国内航线的城市/机场三字码输入。
  - 当前状态：已部署到 Supabase，但 Amadeus Self-Service 新注册入口已不可用；仅旧账号可能继续使用。
  - 当前限制：Amadeus Self-Service portal 已提示将于 2026-07-17 下线，不再作为主线方案。
  - 当前限制：Amadeus 只解决机票报价；动车/高铁仍需 12306 查询、手动保存，或后续接入有授权的火车票 API。
- [~] SearchApi / Google Flights 航班 API
  - 已完成：新增 Supabase Edge Function `searchapi-flight-offers`，用于安全调用 SearchApi 的 Google Flights API。
  - 已完成：前端接入面板改为 Google Flights 报价接入，默认代理地址切到 `searchapi-flight-offers`。
  - 已完成：`SEARCHAPI_API_KEY` 已配置到 Supabase secrets。
  - 已完成：已用 `SHA -> LHW`、`2026-07-10` 测试国内航线，成功返回 10 条 Google Flights 航班报价。
  - 已完成：Google Flights / SearchApi 返回的航班结果会保存为当天协作报价，写入计划级 Yjs Array；其他协作者和重新打开页面后都能看到同一批报价。
  - 当前限制：这是聚合搜索接口，不是航空公司或 12306 官方接口；适合先做航班价格可视化验证。
- [~] 多出发地综合比较
  - 已完成：可填写多位成员的出发地/机场码，例如 `林: SHA; 王: PEK; 周: CAN`。
  - 已完成：按同一到达地、同一日期和时间筛选逐个查询 Google Flights / SearchApi，并展示每人的最低价、航班、耗时、经停和推荐等级。
  - 已完成：多人出发地比较完成后，会把每位成员的推荐最低航班保存为当天协作报价，其他协作者和重新打开页面后可在交通列表看到。
  - 已完成：多人出发地比较的异步流程增加错误兜底；即使查询或保存失败，比较按钮也会恢复可用，避免卡在加载状态。
  - 当前限制：每个出发地会消耗一次 SearchApi 查询；当前最多比较 6 个出发地。
  - 当前限制：暂时只覆盖航班；动车/高铁仍需要 12306 查询或手动保存。
- [ ] 真实 Trip.com/Ctrip 机票/动车 API
  - 未完成原因：需要具体产品 API 权限、AppKey/AppSecret、接口文档。
  - 当前判断：个人账号大概率无法直接申请完整机票/火车票 API。

### 2. AI 优化路径

- [~] 本地路径排序
  - 已完成：按地点坐标/经纬度做简单距离排序。
- [~] AI 代理接口入口
  - 已完成：可填写 AI 路径代理地址和访问令牌。
- [~] Supabase AI 路径代理
  - 已完成：新增并部署 Edge Function `ai-route-optimize`。
  - 已完成：前端默认接入 Supabase AI 代理，未配置模型密钥时返回兜底顺序。
  - 已完成：前端会把日期、节奏、预算、天气、地点类型、备注、标签、经纬度一起传给代理。
  - 已完成：已在 Supabase Secrets 配置 TokenFlux 的 OpenAI 兼容模型密钥、Base URL 和模型名。
- [~] AI 优化结果展示
  - 已完成：页面会展示优化来源、推荐顺序、优化说明、分段交通建议和风险提示。
  - 已完成：模型未配置或调用失败时明确显示“本地兜底”，避免误认为真实 AI 结果。
- [~] 真实 AI 路线规划
  - 已完成：TokenFlux 新建 `tripboard-supabase` 专用 key，使用 `阿里云百炼（OpenAI格式）` 分组，避免旧 Codex 专用 key 的 TLS 路由限制。
  - 已完成：TokenFlux 新建 `tripboard-gpt-test` 专用测试 key，使用 `ChatGPT Plus (兜底)` 分组，已测通 GPT 系模型。
  - 已完成：当前使用 `https://tokenflux.dev/v1` + `gpt-5.5`，后端已实测返回 `OpenAI compatible · gpt-5.5`。
  - 已完成：最新版 `ai-route-optimize` 已部署到 Supabase，并已实测返回 `order`、`note`、`reason`、`segments`、`warnings`。
  - 已完成：后端会用真实地点标题修正 `segments.from/to`，避免模型或请求编码导致分段地点名显示异常。
  - 已完成：模型提示词已要求考虑地理位置、固定时间、餐点、预约/开放提示、天气、折返、体力、预算和标签。
  - 待实现：自动接入营业时间、实时交通、排队数据和景区拥挤度；目前这些仍依赖地点备注/标签或后续 API。

### 3. 高德地图定位

- [~] 高德搜索链接
  - 已完成：可生成高德搜索链接。
- [~] 经纬度录入
  - 已完成：地点可填写经纬度。
- [~] 高德代理接口入口
  - 已完成：可填写高德地点代理地址和高德路线代理地址。
- [~] 高德 Web 服务地点搜索代理
  - 已完成：新增并部署 Edge Function `amap-place-search`。
  - 已完成：前端默认接入 Supabase 高德代理，快速录入和地点详情可调用代理回填地址、经纬度。
  - 已完成：已在 Supabase Secrets 配置 `AMAP_WEB_SERVICE_KEY`，并用“甘肃省博物馆”“兰州中山桥”测通真实地点搜索。
- [x] 高德 Web 服务路线规划代理
  - 已完成：新增并部署 Edge Function `amap-route-plan`，支持步行、驾车、公交/地铁。
  - 已完成：前端可按当前天地点顺序请求高德路线，展示总距离、总耗时和每段说明，并保存到协作计划。
  - 已完成：路线规划前会尝试用高德地点搜索补齐缺失经纬度。
  - 已完成：已用甘肃省博物馆到黄河铁桥测试真实高德驾车路线，返回约 6.5 km、约 16 分钟。
- [~] 高德地图 SDK
  - 已完成：前端新增高德 JS Key 和 `securityJsCode` 配置入口；配置 Web 端 JS API Key 后，右侧地图会动态加载高德 JS SDK 并渲染当天地点标记。
  - 已完成：高德路线规划代理返回的路线分段会保留 polyline 坐标；地图 SDK 可用时会优先绘制真实路线折线，未配置 JS Key 时保留本地坐标预览。
  - 当前限制：需要另行申请“Web 端 JS API”Key，不能直接复用后端 Web 服务 Key；未填写 JS Key 时不加载真实高德底图。
- [x] 真实地点搜索/路线规划 API
  - 已完成：地点搜索和路线规划 Web 服务代理已完成并测通。
  - 已完成：快速录入和地点详情都支持展示高德返回的候选地点列表，可选择具体候选后回填地址、经纬度并写入协作字段。
  - 已完成：路线规划支持路线偏好参数；驾车可选时间优先、少收费、躲避拥堵，公交/地铁可选时间优先、少步行、少换乘。
  - 已完成：路线规划失败后会显示重试按钮，复用上一次交通方式和路线偏好重新请求。
  - 当前限制：地点候选、路线和底图能力仍依赖高德 Key 的配额、应用类型和高德 Web 服务可用性；官方 POI 图片、营业时间、实时拥堵细节仍需后续单独数据源或高德对应权限。

### 4. 外部 App 同步

- [x] 手动导入入口
  - 已完成：美团/点评、民宿/酒店、截图/分享都有导入入口。
  - 已完成：可选择类别、日期，填写名称、时间、金额、已付、付款人、地址、订单号、来源链接和备注。
- [x] AI 辅助文本解析
  - 已完成：新增并部署 Edge Function `external-order-parse`。
  - 已完成：前端默认接入 Supabase 解析代理，粘贴订单短信、邮件、分享文本后可解析名称、日期、时间、金额、已付、地址、订单号和备注。
  - 已完成：解析结果会先显示预览，再由用户确认导入。
  - 已完成：AI 慢响应或失败时会自动切到本地规则解析，避免按钮长时间卡住。
- [x] 导入到协作计划
  - 已完成：确认导入后会新增到匹配日期；订单日期不在行程范围内时导入当前天并在预览里提示。
  - 已完成：导入记录会写入预算、已付、付款人、来源、类别、订单号、解析来源和原文，并通过 Supabase 同步给协作者。
- [ ] 自动读取外部 App 订单
  - 未完成：需要平台授权/API。
  - 当前限制：不能直接读取用户手机 App 或账号订单。
  - 当前判断：个人网页不能绕过美团/点评、酒店/民宿、OTA 平台授权；后续只能接官方开放接口、企业接口，或继续使用用户主动粘贴/分享导入。

### 5. 多人协作冲突处理

- [x] 实时同步
  - 已完成：Supabase Realtime 同步计划。
- [x] 历史版本/恢复
  - 已完成：编辑前、本地/远端更新前保存历史版本。
  - 已完成：历史版本列表会对比当前计划显示差异摘要，提示天数、地点、协作块、批注、交通报价、备选和预算的增减，恢复前更容易判断版本内容。
  - 已完成：恢复历史版本改为两步确认；点击版本先显示恢复预览、具体变化范围和“确认恢复 / 取消”，避免误点旧版本覆盖当前协作内容。
  - 已完成：恢复预览会显示当前在线协作者及其正在浏览/编辑的位置，提醒恢复操作会影响正在协作的成员。
  - 已完成：确认恢复历史版本后会广播专用协作通知，其他在线成员会看到“某成员恢复了历史版本”及版本摘要，而不是只收到普通整份计划更新。
  - 已完成：历史版本记录会保留当时的 `planYjs` 协作快照，但版本摘要和去重仍按可见内容计算；恢复带快照的历史版本时会优先装载该快照为 live Yjs 文档，失败时才从 JSON 重建。
  - 已完成：恢复历史版本会写入活动流；恢复者和收到广播的成员都会留下带版本摘要的活动记录，方便之后追溯计划为何回退。
  - 已完成：活动流支持按类型筛选并显示类型标签，可区分恢复、冲突、批注、交通、路线和普通编辑记录，协作事件更容易回看。
  - 已完成：活动记录支持点击定位；批注、交通、路线、恢复、冲突和编辑类记录会跳转到对应面板并短暂高亮。
  - 已完成：新产生的地点评论、当天批注和协作块评论活动会保存具体批注 `commentId`，点击活动可直接定位到对应线程，而不仅是跳到面板。
  - 已完成：新增、完成/重新打开、排序、拖拽排序和删除协作块的活动会保存 `dayId/blockId`，点击活动可切到对应日期并定位协作块；目标已删除时会定位到当天协作块区域。
  - 已完成：删除地点评论、当天批注和块级评论的活动会保存原位置；点击记录时即使原线程已删除，也会回到对应地点评论区、当天批注总览或协作块区域。
  - 已完成：保存当天设置、新增/删除/移动日期、保存/新增/删除/移动地点、快速加入景点、备选加入当天和外部导入地点的活动会保存 `dayId/stopId`，点击活动可直接切到对应日期或地点；目标被删除时会回到原日期时间线。
  - 已完成：远程协作者新增/删除/排序日期或地点、更新当天设置时，本端活动流也会保存对应 `dayId/stopId`，点击记录可跳到协作者刚改动的位置或相邻兜底位置。
  - 已完成：候选池新增/更新/删除、手动交通报价新增/更新/删除、Google Flights 同步和预算设置活动会保存具体目标；点击活动可回到对应备选卡片、交通报价/当天交通面板或预算输入项。
  - 已完成：目的地和出发地更新会写入活动流并保存字段目标；点击活动可回到对应向导输入框。
  - 已完成：出发日期、返程日期和日期范围更新会写入活动流并保存字段目标；点击活动可回到对应日期输入框。
- [x] 冲突检测/解决面板
  - 已完成：保存时使用云端 `updated_at` 做版本校验；若本地和云端同时修改，不再静默覆盖，会提示“智能合并 / 保留我的 / 使用云端”。
  - 已完成：智能合并会尽量保留双方新增地点、评论、交通报价、备选地点和活动记录；处理前会把双方版本写入历史版本。
  - 已完成：冲突面板新增“我的修改 / 云端修改 / 同位置冲突”摘要，按行程、地点、协作块、批注、交通报价和备选地点列出变化范围，处理冲突前可以先判断合并风险。
- [x] 字段级合并
  - 已完成：冲突面板的“智能合并”使用上次同步版本、本地版本、云端版本做三方合并；不同成员修改同一地点的不同字段时会自动合并。
  - 当前限制：同一个字段被两个人同时改动时优先保留本地版本，并把云端版本写入历史；不是 Notion 那种逐字实时合并。
- [x] 编辑锁/正在编辑提示
  - 已完成：在线成员会显示当前正在浏览/编辑的日期和地点。
  - 已完成：基于 Supabase Presence 做地点级编辑锁；一个成员正在编辑某个地点时，其他成员打开同一地点会看到锁定提示，表单、保存、删除、移动和高德回填会临时禁用。
- [~] Notion 式逐字协作
  - 已完成：地点“备注”字段接入 Yjs CRDT，支持同一地点备注逐字实时广播同步。
  - 已完成：已扩展为地点文本字段组协作，覆盖名称、类型、地址、高德关键词、备注。
  - 已完成：文本字段组的 CRDT 状态会随计划保存到 Supabase，后加入的成员可以接上同一份文本协作文档。
  - 已完成：地点级编辑锁只锁定移动、删除、高德回填等操作；已接入 CRDT 的地点详情字段仍可多人协作。
  - 已完成：基于 Supabase Presence 显示协作者正在编辑的文本字段、光标位置和选区长度；字段旁会出现带彩色光标条/选区条的协作者提示。
  - 已完成：文本字段远程光标升级为字段内联覆盖层，会在名称、类型、地址、高德关键词和备注输入区域内显示协作者彩色光标、选区条和姓名首字母，同时保留字段下方的协作者提示。
  - 已完成：地点详情中的时间、预算、已付、付款人、经纬度、图片 URL、标签已接入同一份 Yjs 协作文档，支持实时同步并随计划保存。
  - 已完成：地点详情中的时间、预算、已付、付款人、经纬度、图片 URL、标签接入 Presence 光标/选区提示；协作者编辑这些结构字段时，字段旁会显示彩色光标和选区状态。
  - 已完成：当前地点评论已接入 Yjs Array，多个成员同时新增评论会实时合并显示，并随计划保存。
  - 已完成：当前地点评论支持协作删除；删除评论会从 Yjs comments Array 按 `id` 移除，并刷新计划级地点快照。
  - 已完成：当前地点评论支持字段/选区锚点；在名称、类型、地址、高德关键词或备注里定位/选中文字后发表评论，会保存评论挂载位置、选区范围和文本摘要，点击评论锚点可回到对应字段位置。
  - 已完成：当前地点评论升级为轻量线程；主评论可回复、标记已解决或重新打开，回复作为独立 Yjs 评论记录追加保存，多人同时回复同一条评论时会合并显示。
  - 已完成：带锚点的未解决批注会在对应字段旁显示计数标记；评论区支持按“全部 / 未解决 / 已解决”筛选，点击字段批注标记可定位到对应评论线程。
  - 已完成：结构字段也支持批注锚点和字段标记，覆盖时间、预算、已付、付款人、经纬度、图片 URL 和标签；这些字段的未解决批注同样会显示计数并可定位。
  - 已完成：带锚点的批注会在输入框内部显示淡色选区高亮；点击高亮可定位到对应评论线程，未解决和已解决批注用不同弱提示状态区分。
  - 已完成：手动新增地点、快速录入景点、加入备选地点、外部订单导入会实时广播给协作者；同一行程日内新增地点可即时合并显示。
  - 已完成：地点删除和上移/下移会实时广播给协作者，其他成员的时间线会同步删除或重排并保留当前选中地点。
  - 已完成：新增、删除、上移/下移行程日会实时广播给协作者；当天日期、标题、路线、天气、交通字段可直接编辑并同步。
  - 已完成：当天日期、标题、路线、天气和交通字段在表单 change 时会直接写入计划级 Yjs Array、保存云端并广播给协作者，减少等到点击保存才同步造成的覆盖窗口。
  - 已完成：当天标题、路线、天气和交通字段增加输入防抖协作同步；输入时会安静写入计划级 Yjs 并保存云端，日期字段仍在 change 后同步以避免中间状态重排行程。
  - 已完成：生成推荐计划、生成空白模板、重置示例会广播整份计划替换，让已打开同一链接的成员即时看到结构变化。
  - 已完成：手动交通报价列表接入计划级 Yjs Array；多个成员同时保存航班/动车报价时会实时合并，并随计划保存到云端。
  - 已完成：Google Flights / 多出发地等批量报价保存改为直接向计划级 Yjs 报价数组追加去重项，不再先拼整份本地报价列表后整体同步，减少批量同步覆盖其他成员手动新增报价的窗口。
  - 已完成：备选池接入计划级 Yjs Array；快速录入可直接加入备选池，多人同时新增备选景点会实时合并，并随计划保存到云端。
  - 已完成：同行人数和预算上限接入计划级 Yjs Map；预算面板设置可被多人实时协作修改，并随计划保存到云端。
  - 已完成：地点互动字段接入 Yjs 协作结构；收藏会实时同步，必去投票按成员记录投票人列表，多个成员同时投票不会互相覆盖。
  - 已完成：计划元信息接入计划级 Yjs Map，覆盖行程名、目的地、出发地、日期范围、封面、同行人数和预算上限；生成/重置计划后会重建计划级协作文档，避免旧协作数组串入新计划。
  - 已完成：活动记录接入计划级 Yjs Array；多个成员同时操作时活动流会合并保存，不再只依赖最后一次整份计划保存。
  - 已完成：行程日元数据接入计划级 Yjs Array，覆盖每天的日期、标题、路线、天气、交通和高德路线摘要；保存当天设置、新增/删除/排序行程日、生成/重置计划时会同步日级结构快照。
  - 已完成：地点列表结构接入计划级 Yjs `stopLists`；新增/删除地点使用 Yjs Array 局部插入/删除，排序和路线重排写入持久结构，后加入或漏掉瞬时广播的成员也能恢复地点列表和顺序。
  - 已完成：计划级 Yjs 文档快照会编码保存到计划 JSON，并在重新打开共享链接时优先恢复；如果快照与可见计划 JSON 不匹配，会自动回退为从 JSON 重建，避免旧快照覆盖新计划。
  - 已完成：每个地点的文本/结构/评论协作快照新增计划级 Yjs `stopTextStates` 集中索引；地点文本变化会同步写入计划级文档，重新打开或漏掉瞬时广播时可从计划级快照恢复地点协作文档。
  - 已完成：地点详情 CRDT 字段变化时会同步刷新计划级 `stopLists` 中的对应地点快照，后加入成员或重新打开链接时能恢复最新名称、地址、备注、预算、坐标、投票、评论等可见详情。
  - 已完成：生成推荐计划、生成空白模板、重置示例和恢复历史版本时会清理旧计划级 Yjs 快照，并重新同步日程和地点列表结构。
  - 已完成：整份计划替换入口统一走计划级 Yjs 重建流程；历史版本恢复、冲突合并/保留本地、生成推荐计划、空白模板和重置示例都会重建并保存新的计划级协作文档，避免旧 CRDT 快照残留。
  - 已完成：行程日元数据和地点列表的计划级 Yjs Array 改为按 `id` 局部同步；编辑、排序、新增或删除时尽量移动/替换单项，不再每次整段删除重插，减少多人同时操作时的结构覆盖。
  - 已完成：当天设置保存改为字段级 patch 写入计划级 Yjs；多人同时修改同一天的标题、路线、天气、交通等不同字段时，会基于 Yjs 最新值只替换自己改动的字段，减少整条 day 元数据覆盖对方修改。
  - 已完成：交通报价、备选池和活动流新增计划级 Yjs Array 同步函数；整份计划替换、冲突合并/保留本地、历史恢复和 fallback 保存会同步这些数组，减少仅靠整份 JSON 保存造成的覆盖风险。
  - 已完成：交通报价和备选池支持协作删除/移除；手动报价删除、备选地点移除会通过计划级 Yjs Array 按 `id` 删除同步，其他协作者会实时看到列表变化。
  - 已完成：交通报价和备选池支持协作编辑/更新；手动报价可回填修改班次、时间和价格，备选地点可回填快速录入区修改名称、地点、时间和预算，更新会按 `id` 替换 Yjs Array 中的单项，避免整份列表覆盖。
  - 已完成：天气同步写入行程日元数据的计划级 Yjs Array；同步天气后会广播每一天的天气字段并保存新的协作快照，避免只靠整份 JSON 保存覆盖其他成员的日程修改。
  - 已完成：地点评论、手动交通报价和备选池新增在协作写入成功后会立即刷新当前页面、记录活动并清空输入，不再等待远端回流才显示结果。
  - 已完成：投票、收藏、评论、备选池、手动交通报价、同行人数和预算上限在 Yjs 协作写入成功后会立即保存云端，不再只依赖延迟保存计时器；刷新过快时丢失协作结果的风险进一步降低。
  - 已完成：本地活动记录和计划级 Yjs 活动流使用同一个活动 `id` 合并，避免同一次操作在活动栏里重复出现。
  - 已完成：远程广播事件写入本地活动栏时不会再次广播到协作活动流，避免“协作者做了某事”的活动记录在多端之间来回回声重复。
  - 已完成：高德路线规划和 AI/本地路径优化改为先写入计划级 Yjs 快照，再保存云端 JSON，避免路线排序或高德坐标更新只进入可见 JSON 而协作快照滞后。
  - 已完成：计划级 Yjs 更新新增本地离线队列；网络失败时会按共享计划暂存本地结构更新，重新载入共享计划、实时通道恢复、浏览器恢复在线或收到协作者更新后会重放并尝试同步到云端。
  - 已完成：新增/删除/排序行程日、保存当天设置、新增/删除/排序地点、快速加入景点、备选加入当天和外部导入地点改为先写入计划级 Yjs，再保存云端 JSON；旧广播仅作为即时通知兼容层，进一步减少整份保存先覆盖协作结构的窗口。
  - 已完成：整份计划替换广播新增计划级 Yjs 快照；恢复历史版本、生成推荐计划、生成空白模板和重置示例会先重建/保存协作快照，再广播 `planYjs`，接收端优先从 Yjs 快照恢复，旧整份 JSON 只作为兜底。
  - 已完成：收到整份计划替换广播时，接收端会把远端 `planYjs` 直接装载为当前 live 计划协作文档并复用同一套更新监听；不再先用临时文档读快照、再销毁重绑，减少历史恢复/生成模板这类替换操作中的中间状态窗口。
  - 已完成：载入共享计划、采用云端版本和无本地修改时应用远端计划，也会优先把远端 `planYjs` 装载为当前 live 计划协作文档；只有快照缺失或恢复失败时才回退到 JSON 应用和重绑。
  - 已完成：旧共享计划如果还没有 `planYjs`，首次以可编辑方式载入后会从现有 JSON 自动生成计划级 Yjs 快照并静默保存回 Supabase；后续打开会直接走协作快照路径。
  - 已完成：导出 JSON 前会先刷新当前地点文本、当天文本和计划级 live Yjs 协作快照；导出的文件会携带最新 `planYjs`，避免只导出可见 JSON 而丢掉协作文本/结构状态。
  - 已完成：新增导入 JSON 入口；导入由 Tripboard 导出的 JSON 时会优先恢复其中的 `planYjs` 为 live 协作文档，快照缺失或失败时才从 JSON 重建协作结构，并同步给共享计划成员。
  - 已完成：恢复历史版本、生成推荐计划、生成空白模板和重置示例的收尾保存也改为计划协作保存入口；整份替换后会先刷新计划级 Yjs 快照再写入 Supabase。
  - 已完成：行程日和地点的新增、删除、排序、当天设置广播都会携带计划级 `planYjs` 快照；接收端会先把快照合并进 live Yjs 文档并检查目标变更是否已出现，只有快照缺失或不匹配时才走旧局部 JSON 兼容路径。
  - 已完成：Supabase 表记录的云端更新也会优先合并保存进 JSON 的 `planYjs` 快照；带协作快照的远端保存不再直接进入旧 JSON 冲突路径，快照缺失或合并失败时才回退到冲突面板。
  - 已完成：旧兼容广播里的计划元信息改为跟随 `PLAN_SETTING_FIELDS`，编辑口令哈希/提示等权限元信息也会随计划级 Yjs Map 和兼容 payload 同步，不再只同步行程名、目的地等基础字段。
  - 已完成：必去投票、收藏、评论新增/删除的 fallback 路径改为先刷新计划级地点快照，再保存云端；即使当前地点文本协作文档未绑定成功，也会尽量通过计划级 Yjs 恢复互动状态。
  - 已完成：地点详情保存优先同步计划级 Yjs 中的单个地点快照，只有失败时才回退到整天地点列表同步，减少多人同时编辑不同地点时的覆盖窗口。
  - 已完成：手动交通报价和备选池的 fallback 保存会先读取最新计划级 Yjs 列表，再按新增、更新或删除的单项合并；即使主协作写入临时失败，也会尽量避免用本地旧列表整段覆盖其他成员刚保存的记录。
  - 已完成：地点评论和当天批注的 fallback 保存改为走协作文本保存入口；保存前会刷新当前地点/当天文本和计划级 Yjs 快照，减少批注备用路径用旧 JSON 覆盖协作内容的窗口。
  - 已完成：协作块新增、勾选、排序、拖拽、删除、块级评论和块文字编辑的 fallback 保存改为走计划协作保存入口；写入 dayBlocks 快照后再保存云端 JSON。
  - 已完成：手动交通报价、备选池和预算设置的 fallback 路径不再先触发普通整份保存；会先写入计划级 Yjs/元信息，再保存云端 JSON，进一步减少旧状态覆盖协作者新修改的窗口。
  - 已完成：向导里的目的地和出发地输入会防抖写入计划级 Yjs Map，并保存云端；多人同时打开同一计划时，基础行程属性不再只停留在本机向导状态。
  - 已完成：行程名、目的地、出发地、封面 URL 和编辑口令提示升级为计划级 `Y.Text` 文本文档；输入目的地/出发地时会同步写入 CRDT 文本和普通设置值，后加入成员可从 `settingTextStates` 恢复同一份文本快照。
  - 已完成：目的地、出发地、出发日期和返程日期输入接入 Presence 光标/选区提示；协作者编辑计划基础信息时，向导输入框内会显示彩色光标和选区状态。
  - 已完成：向导里的出发日期、返程日期和日期范围会作为同一个 patch 写入计划级 Yjs Map，并保存云端；仅同步计划基础属性，不会自动重排已有每日行程。
  - 已完成：出发日期、返程日期和日期范围也升级为计划级 `Y.Text` 文本快照；日期 patch 会同时刷新普通设置值和 CRDT 文本状态，避免重新打开共享链接时日期字段只依赖最后一次普通覆盖。
  - 已完成：地点上移/下移、AI 路径优化和高德路线回填改为优先重排或 patch 计划级 Yjs 中已知地点，并保留协作文档里本地尚未刷新的新增地点，减少路线重排误删他人新增地点的风险。
  - 已完成：天气同步、Google Flights/多出发地报价、当天设置、行程日/地点增删排序、候选加入当天、外部导入、编辑口令和路线优化等结构性操作的收尾保存改为协作保存入口；保存前会刷新文本和计划级 Yjs 快照，不再直接走普通整份保存。
  - 已完成：行程日删除改为专用 Yjs 删除，只移除当天元数据、当天地点列表和对应地点文本快照；行程日上移/下移不再重写全部地点列表，避免日期排序时覆盖其他日期的新地点。
  - 已完成：收到云端更新并进入冲突判断前、以及执行冲突合并前，会先把当前 live Yjs 协作结构同步回可见计划，降低合并时漏掉本端最新协作字段、报价、备选池或活动记录的风险。
  - 已完成：冲突面板里的“智能合并”在云端版本带有计划级 `planYjs` 快照时，会优先把远端协作快照合入本地 live Yjs 文档并统一保存；合并失败时才回退到旧 JSON 字段合并，减少冲突解决时重建整份协作文档造成的逐字历史丢失。
  - 已完成：旧 JSON 字段合并兜底路径会基于合并后的计划重新生成计划级 `planYjs` 快照；若运行环境无法生成快照，才清除旧快照，避免把不匹配的协作状态继续保存。
  - 已完成：新增行程日改为局部插入 dayMeta；行程日上移/下移改为只重排行程日元数据，并保留协作文档里本地尚未刷新的新增日期，减少日期排序误删其他成员新增日期的风险。
  - 已完成：天气同步和高德路线摘要改为按行程日字段 patch 写入 Yjs，不再为了单个天气或路线结果重写整份行程日元数据。
  - 已完成：当天标题、路线、天气和交通字段接入独立 Yjs 文本文档，支持同一天设置里的逐字 CRDT 合并；文本快照会写入计划级 `dayTextStates`，重新打开共享链接后可从协作快照恢复。
  - 已完成：当天标题、路线、天气和交通字段接入 Presence 光标/选区提示；协作者编辑同一天设置时，字段旁会显示彩色光标和选区状态。
  - 已完成：远程文本光标/选区从百分比估算升级为基于当前输入框字体、宽度、滚动位置和换行的像素坐标测量，名称、类型、地址、高德关键词和备注字段的协作者光标更接近真实文字位置。
  - 已完成：地点文本/结构/评论变化会刷新计划级文本快照索引；云端保存失败时会显示文本协作已进入计划级离线队列，离线队列重放成功后会再次刷新当前地点文本快照。
  - 已完成：所有云端保存入口在写入 Supabase 前会先把当前地点文本、当天文本和计划级 live Yjs 协作文档刷新回可见 `state`，减少普通保存用旧 JSON 覆盖最新协作结构的风险。
  - 已完成：保存前刷新护栏会等待当前地点文本快照写入计划级 `stopLists/stopTextStates` 后再生成云端 payload，避免异步快照尚未落盘时就保存旧 JSON。
  - 已完成：当天标题、路线、天气和交通字段支持协作批注；批注可锚定到字段/选区，支持回复、解决/重新打开、删除和筛选。
  - 已完成：当天批注写入当天 Yjs 文本文档和计划级 `dayMetas`，多人同时追加会合并，刷新共享链接后可恢复。
  - 已完成：当天字段的批注会显示字段计数标记和输入框内部高亮，点击高亮或标记可定位到对应当天批注线程。
  - 已完成：新增按天“协作块”内容模型，支持待办、备注、决定三类块；块的新增、文本编辑、完成/重新打开和删除写入计划级 Yjs `dayBlocks`。
  - 已完成：协作块会随计划级 Yjs 快照保存和恢复；生成/重置/历史恢复/冲突处理时会同步 dayBlocks，其他协作者和重新打开共享链接后可恢复同一批块。
  - 已完成：协作块支持上移/下移排序，排序结果写入同一份计划级 Yjs `dayBlocks`，多人打开共享链接后可恢复块顺序。
  - 已完成：协作块支持块级评论、回复、解决/重新打开、删除和筛选；评论嵌入块对象并随 `dayBlocks` 协作快照保存和恢复。
  - 已完成：协作块支持块级编辑状态 presence；成员聚焦、编辑、评论或操作协作块时，会在成员列表和对应块旁显示“正在编辑/评论协作块”的提示。
  - 已完成：协作块支持桌面端拖拽排序；拖拽手柄重排后会写入计划级 Yjs `dayBlocks`，并保留原有上移/下移按钮作为可访问兜底。
  - 已完成：协作块文本接入计划级 Yjs `dayBlockTexts`/`dayBlockTextStates`；块文字编辑会写入独立 `Y.Text`，并随计划级快照保存、恢复和广播，减少多人同时编辑同一块时的整字段覆盖。
  - 已完成：协作块文字编辑会写入活动流并保存 `dayId/blockId`，点击活动可回到对应块继续查看或修改。
  - 已完成：协作块文本支持远程光标/选区 Presence；成员聚焦或选中块文字时，其他成员可在对应块输入框内看到彩色光标、选区条和姓名首字母。
  - 已完成：协作块评论支持锚定到块文字选区；选中文字后新增块级评论会保存 `scope=block` 锚点，输入框内显示批注高亮，点击高亮或评论锚点可回到对应文字位置。
  - 已完成：新增全局“批注总览”侧栏，聚合地点批注、当天批注和协作块批注；可按未解决/全部/已解决筛选，并点击跳转到对应日期、地点、协作块或字段锚点。
  - 已完成：批注总览支持直接“解决 / 重新打开”地点、当天和协作块批注；操作会写回对应协作数据并保存云端，减少必须跳回原字段才能处理批注的摩擦。
  - 已完成：批注总览支持直接回复地点、当天和协作块批注；回复会追加到对应线程并同步云端，形成更接近 Notion 侧边栏的批注处理流程。
  - 当前限制：目前覆盖地点详情字段、评论、地点文本快照索引、地点列表结构、行程日元数据、行程日文本快照、计划元文本快照、交通报价、备选池和部分计划设置，并已持久化计划级 Yjs 快照；尚未把整份计划迁移成单一 Yjs Map/Array 数据模型。
  - 当前限制：远程光标已按普通 input/textarea 文本排版测量，评论也能挂到文本字段和结构字段并形成轻量线程，字段旁可显示未解决批注，字段内部可显示批注选区高亮，并已有全局批注索引；但仍不是富文本编辑器那种嵌入式块级光标、多人选区拖拽和块级内容编辑体验。
  - 当前限制：尚未实现整份计划结构 CRDT、离线后的完整自动合并；当前离线重放覆盖计划级 Yjs 更新队列，非 Yjs 的旧广播/整份替换类操作仍需继续迁移。

### 6. 成员权限

- [x] 编辑链接
  - 已完成：普通共享链接可编辑。
- [x] 只读链接
  - 已完成：`mode=readonly` 链接只能查看，不能修改计划。
- [x] 轻量编辑口令
  - 已完成：共享计划可设置编辑口令；未通过口令验证的编辑链接会降级为只读状态，输入正确口令或使用带 `editKey` 的编辑链接后才能修改。
  - 当前限制：这是前端协作门槛，不是完整账号/管理员权限；真正强权限仍需 Supabase Auth + RLS。
- [ ] 登录系统
  - 未完成：没有账号登录。
- [ ] 管理员/成员角色
  - 未完成：没有管理员、成员、访客等角色分级。
- [ ] 邀请审批
  - 未完成：拿到编辑链接的人仍可编辑。

### 7. 在线成员

- [x] 成员信息填写
  - 已完成：用户填写姓名、身份/备注后加入协作。
- [x] 在线成员展示
  - 已完成：通过 Supabase Presence 展示在线成员。
- [x] 公网同步
  - 已完成：已推送公网。

### 8. 预算

- [x] 项目预算汇总
  - 已完成：按地点预算汇总。
- [x] 分类预算
  - 已完成：交通、餐饮、门票、住宿分类汇总。
- [x] 已付/待付
  - 已完成：地点可记录已付金额。
- [x] 付款人
  - 已完成：地点可记录付款人。
- [x] 同行人数/人均
  - 已完成：可填写同行人数并计算人均。
- [x] AA 结算/转账建议
  - 已完成：根据同行人数、项目总预算、已付金额和付款人汇总生成“谁转给谁”的 AA 结算建议；付款人缺失时会提示先补充付款人。

### 9. 天气

- [x] Open-Meteo 天气同步
  - 已完成：不填代理时可用 Open-Meteo 同步天气。
- [x] 天气代理接口入口
  - 已完成：可填写自有天气代理；前端已默认接入 Supabase `amap-weather` 代理。
- [x] 高德天气 Web 服务代理
  - 已完成：新增 Edge Function `amap-weather`，使用 `AMAP_WEB_SERVICE_KEY` 调用高德行政区查询和天气预报接口。
  - 已完成：同步天气时会按行程日期精确匹配高德返回日期，避免把短期天气误填到不对应的未来日期。
  - 当前限制：高德 Web 服务天气主要适合近期天气/预报，不是长期旅行季节预测；超过接口返回日期的行程日不会强行写入天气。
- [ ] 更多国内/商业天气源
  - 未完成：未接入和风天气、彩云天气等更多天气源。

### 10. 图片

- [~] 默认图片
  - 已完成：默认使用 Unsplash 示例图。
- [x] 地点图片 URL
  - 已完成：每个地点可手动填写图片 URL。
- [ ] 自动获取景点官方图
  - 未完成：需要景点数据源、图库 API 或人工上传流程。

## 后续优先级建议

1. 先完成 AA 结算/转账建议，因为不依赖第三方平台授权。
2. 再做成员权限分级，包括登录、管理员、成员、访客和邀请审批。
3. 再接高德地图 SDK，因为能显著提升路线和定位体验。
4. 继续扩展 Notion 式协作，从地点文本字段组升级到多人光标、选区和更多结构字段的 CRDT 合并。
5. 真实携程/Trip.com 票价 API 等拿到企业权限后再接。
